<%= @comment.creator&.full_name || "Ein gelöschter Benutzer" %> hat auf deinen Kommentar zu "<%= @comment.title %>" geantwortet.

-----------------------------------------------------------
<%= @comment.body %>
-----------------------------------------------------------

Um die Diskussion anzusehen, kopiere die folgende URL und füge sie in deinen Browser ein:
<%= @comment.subject_url %>
