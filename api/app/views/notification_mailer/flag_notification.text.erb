Der folgende Inhalt (<%= @kind %>) in "<%= @resource.project.title.titleize %>" wurde <%= @resource.flags_count %>-mal gemeldet.

-----------------------------------------------------------
<%= @resource.body %>
-----------------------------------------------------------

<% if @message %>
Benutzernachricht: <%= @message %>
<% end %>

Um den Inhalt anzusehen, kopiere die folgende URL und füge sie in deinen Browser ein:
<%= @resource.subject_url %>
